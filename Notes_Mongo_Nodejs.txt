Get started a Node JS project
take from https://www.freecodecamp.org/news/how-to-build-a-todo-app-with-react-typescript-nodejs-and-mongodb/#api-with-nodejs-express-mongodb-and-typescript
npm init -y

Express (Web Server)
   Install dependency
   Create express app instance
   
   npm install express
   
 
Nodemon(Lets you local server live reload)
npm install nodemon --save 


Middleware 
 Access to request/response objects and the next middleware function in the pipeline
 Execute any code
 Call the next middleware function in the stack
 
 
 Mongoose 
  Install dependency
  Reference mongoose
  CRUD API
  
  
  
  
  
Todo backend
 Done -Installation and so on 
  Use Typescript, NodeJs, Express and MOngoDB
  
  It's a backend for a todo list App.
  
  Dependencies are:
    npm install express cors mongoose
  
  
  Dev dependencies are:
  npm install concurrently  --save-dev 
  npm install nodemon --save 
  npm install --save-dev @types/node @types/express @types/mongoose @types/cors
  
  
  -Create a todo types
    There's a Todo interface that extends the Document type provided by mongoose. 
	  We will be using it later to interact with MongoDB
 
 -Create TodoModel
 
 
 Create API controllers
 Get, Add, Update and Delete Todos
controllers/todos/index.ts
 
 

